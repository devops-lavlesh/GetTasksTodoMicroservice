apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-get-tasks-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: todo-get-tasks-api
  template:
    metadata:
      labels:
        app: todo-get-tasks-api
    spec:
      containers:
      - name: todo-get-tasks-api
        image: lucktodoappacr2025.azurecr.io/todo-get-tasks-api:v4
        ports:
        - containerPort: 8000
        env:
        - name: CONNECTION_STRING
          value: "Driver={ODBC Driver 18 for SQL Server};Server=tcp:devtodoappsqlsvrlav.database.windows.net,1433;Database=devtodoappdblav;Uid=sqladminlav;Pwd=Devopslav@1001;Encrypt=yes;TrustServerCertificate=no;Connection Timeout=30;"
          

