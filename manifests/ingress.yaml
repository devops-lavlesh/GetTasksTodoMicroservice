apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: todo-get-tasks-api-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: get-task-api.shineluck.shop
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: todo-get-tasks-api
                port:
                  number: 80
